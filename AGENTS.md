
## 代码改动规范

本仓库后续改动必须遵守这些规则：

- 不要保留新旧两套实现
- 相同功能只保留一份更好的实现
- 不要把策略逻辑、市场规则、风控规则和数据处理硬耦合在一个模块
- 不要把大量不同职责堆进一个文件
- 优先复用已有接口和目录结构
- 变更代码后，相关文档必须同步更新

尤其注意：

- 如果实现和文档不一致，先指出差异，再决定改代码还是改文档

## 提交前检查清单

每次完成一轮较大修改，至少检查：

1. 是否引入了重复实现
2. 是否破坏现有 `HS300` 链路
3. 是否漏接 `research_profile`
4. baseline manifest 是否仍按正确股票池解析
5. 并发 / 缓存 / resume 是否仍然生效
6. README / 开发者文档是否同步
7. 相关测试是否补齐

